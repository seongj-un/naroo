[build]
builder = "RAILPACK"
buildCommand = "./gradlew bootJar"

[deploy]
startCommand = "java -jar build/libs/naroo-*.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
